1. packages.json should contain current version of evernote-sdk. Asterix doesn't work (See meteorhacks:npm help).

2. Create settings.json from settings.json.template

3. Run Meteor with:
    meteor update
    meteor run --settings settings.json

